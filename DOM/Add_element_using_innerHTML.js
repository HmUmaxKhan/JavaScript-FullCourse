// Adding new html elements using inner html

const ul = document.querySelector(".todo-list li");
console.log(ul);

// Changing inner html
// ul.innerHTML = "<li>ALi</li>"

// Adding new html elements using inner html

// ul.innerHTML += "ALi"

// But it is not suitable option , we used it to only change the Element